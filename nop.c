#include "monty.h"


/**
 * _nop - function
 * @pile: the pile
 * @line_number: the number
 * Return: void
 */

void  _nop(stack_t **pile, unsigned  int line_number)
{
	UNUSED(pile);
	UNUSED(line_number);
}
